# ADS-B data source — URL of the Pi's tar1090 endpoint
ADSB_URL=http://<pi-ip>/tar1090/data/aircraft.json

# Database password
DB_PASSWORD=changeme

# Optional overrides (defaults shown)
# POLL_INTERVAL=5
# SESSION_TIMEOUT=300
